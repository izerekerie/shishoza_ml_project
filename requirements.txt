# ── Serialization-critical: PINNED to the exact versions the model was
#    trained/pickled with. A saved scikit-learn model only unpickles safely
#    under the same sklearn/numpy/scipy/joblib it was created with — loose
#    bounds let the deploy install a different version and the model then
#    either fails to load or loads silently broken. Bump these ONLY when you
#    retrain and re-save the .pkl in a matching environment.
numpy==2.4.6
scipy==1.17.1
scikit-learn==1.8.0
joblib==1.5.3
pandas>=1.5.0
matplotlib>=3.6.0
seaborn>=0.12.0
rasterio>=1.3.0
geopandas>=0.12.0
folium>=0.14.0
earthengine-api>=0.1.370
geemap>=0.19.0
jupyter>=1.0.0
notebook>=6.5.0
ipykernel>=6.0.0
tqdm>=4.64.0
flask>=2.2.0
flasgger>=0.9.7
pyproj>=3.4.0

# ── Cadastral extraction (citizen PDF/photo upload) ──
PyMuPDF>=1.23.0
pdfplumber>=0.10.0
opencv-python-headless>=4.8.0
pytesseract>=0.3.10
Pillow>=10.0.0
bcrypt>=4.0.0

# ── Production WSGI server (used in Docker + Render) ──
gunicorn>=21.2.0

# ── Load testing (RQ4 delivery) ──
locust>=2.30.0
